<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <com.onedollar.expandablemenu.ExpandableMenu
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center">

        <ImageView
            android:id="@+id/fabSpeed25"
            android:layout_width="@dimen/menu_item_size"
            android:layout_height="@dimen/menu_item_size"
            android:background="@color/bg_menu_item_background_color"
            app:srcCompat="@drawable/ic_help_white_24dp" />

        <ImageView
            android:id="@+id/fabSpeed50"
            android:layout_width="@dimen/menu_item_size"
            android:layout_height="@dimen/menu_item_size"
            android:background="@color/bg_menu_item_background_color"
            app:srcCompat="@drawable/ic_help_white_24dp" />

        <ImageView
            android:id="@+id/fabSpeed75"
            android:layout_width="@dimen/menu_item_size"
            android:layout_height="@dimen/menu_item_size"
            android:background="@color/bg_menu_item_background_color"
            app:srcCompat="@drawable/ic_help_white_24dp" />

        <ImageView
            android:id="@+id/fabSpeed100"
            android:layout_width="@dimen/menu_item_size"
            android:layout_height="@dimen/menu_item_size"
            android:background="@color/bg_menu_item_background_color"
            app:srcCompat="@drawable/ic_help_white_24dp" />
    </com.onedollar.expandablemenu.ExpandableMenu>

</android.support.design.widget.CoordinatorLayout>